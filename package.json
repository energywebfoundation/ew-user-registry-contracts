{
  "name": "ew-user-registry-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "dist/js/index.js",
  "scripts": {
    "test": "npm run prepare-wrapping && npm run build-ts && mocha dist/js/test/",
    "build-ts": "rm -rf dist/js && node_modules/typescript/bin/tsc && cp src/types/*.d.ts dist/js/types/",
    "prepare-wrapping": "rm -rf src/wrappedContracts && mkdir -p src/wrappedContracts && npm run compile && node wrapping.js",
    "start-ganache": "ganache-cli -m 'chalk park staff buzz chair purchase wise oak receive avoid avoid home' -g 0 -l 80000000 -e 1000000 -a 20",
    "compile": "spm compile"
  },
  "repository": {
    "type": "git",
    "url": "https://git.slock.it/ewf/origin/user-registry/ew-user-registry-contracts.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sloffle": "v1.3.1-beta02",
    "web3": "^1.0.0-beta.36",
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.6.28",
    "chai": "^4.1.2",
    "ganache-cli": "^6.1.8",
    "mocha": "^5.2.0",
    "solc": "^0.4.23",
    "tslint": "^5.11.0",
    "typescript": "^2.9.2",
    "typescript-json-schema": "^0.31.0"
  }
}